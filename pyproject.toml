[project]
name = "multidoc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Keerthi G", email = "jovial123keerthi@gmail.com" }
]
requires-python = ">=3.8"
dependencies = []

[project.scripts]
multidoc = "multidoc:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
